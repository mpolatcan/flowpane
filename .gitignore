node_modules/

# Raw pty captures and debug logs from dev/drive.py: scratch from one debugging
# session, regenerated on demand, and full of escape sequences.
dev/*.raw
dev/*.log
dev/latest

# Serena's project index: a cache of this repo, rebuilt on demand.
.serena/

# macOS Finder metadata, written into every directory it opens.
.DS_Store
